<Window x:Class="MVVM.Views.EditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVM.Views"
        mc:Ignorable="d"
        Title="EditView" Height="479" Width="380" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid>

        <Label FontSize="20" FontWeight="Bold" Margin="10,0,57,381">Make</Label>
        <TextBox Background="Black" Foreground="White" BorderThickness="0" Text="{Binding SelectedCar.Make, UpdateSourceTrigger=PropertyChanged}" FontSize="17" Margin="10,53,10,326"/>
        <Label FontSize="20" FontWeight="Bold" Margin="10,108,57,267">Model</Label>
        <TextBox Background="Black" Foreground="White" BorderThickness="0" FontSize="17" Text="{Binding SelectedCar.Model, UpdateSourceTrigger=PropertyChanged}" Margin="10,167,10,212"/>
        <Label FontSize="20" FontWeight="Bold" Margin="10,222,57,153">Year</Label>
        <TextBox Background="Black" Foreground="White" BorderThickness="0" FontSize="17" Text="{Binding SelectedCar.Year, UpdateSourceTrigger=PropertyChanged}" Margin="10,281,10,98"/>
    </Grid>
</Window>
